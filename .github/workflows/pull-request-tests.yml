name: Pull request checks 🔎

on:
  pull_request:

jobs:
  unit-tests:
    name: Unit tests 🐏
    runs-on: ubuntu-latest
    strategy:
      matrix:
        nodeVersion: ["24.x", "25.x"]

    steps:
      - name: Unit tests 🐏
        uses: tchoupinax/repo-config/shared-actions/steps/node/test-unit@7d1c074b91c71778962e63aa03aa53139c6ecb7d
        with:
          nodeVersion: ${{ matrix.nodeVersion }}
