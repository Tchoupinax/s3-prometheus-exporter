name: Pull request checks 🔎

on:
  pull_request:

jobs:
  unit-tests:
    name: Unit tests 🐏
    runs-on: ubuntu-latest
    strategy:
      matrix:
        nodeVersion: ["24.x", "25.x"]

    steps:
      - name: Unit tests 🐏
        uses: tchoupinax/repo-config/shared-actions/steps/node/test-unit@d4827aa8c40d4405843640cbfde20bd15651f852
        with:
          nodeVersion: ${{ matrix.nodeVersion }}
  build:
    name: Check build success 🦫
    runs-on: ubuntu-latest
    strategy:
      matrix:
        nodeVersion: ["24.x", "25.x"]

    steps:
      - name: Check build success 🦫
        uses: tchoupinax/repo-config/shared-actions/steps/node/build@d4827aa8c40d4405843640cbfde20bd15651f852
        with:
          nodeVersion: ${{ matrix.nodeVersion }}
